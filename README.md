# dynamic-pricing-contextual-bandits
Dynamic pricing with contextual bandits from scratch: LinUCB, linear Thompson sampling, regret against a known optimum, and off-policy evaluation (IPS, capped, self-normalised, doubly robust) with support diagnostics — pure Python
